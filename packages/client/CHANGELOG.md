# Changelog

## [Unreleased]

### Added

- Added the experimental runtime-neutral `PiClient` and multi-session `PiSessionClient` API.
- Added the Node-only Unix-domain socket transport factory while keeping the client root runtime-neutral.
